{
  "name": "monorepo-root",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
