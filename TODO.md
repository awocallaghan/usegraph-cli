# Dependencies page

- Remove internal only functionality, we need to rethink how this works (let users identify internal packages at scan time)
- Link to package adoption from the package deep dive section, allow drilling into the other page for more detailed usage/adoption stats

# Component explorer

- ~~Remove the static vs dynamic ratio, this section is not useful~~ ✅
- ~~I want a table of all usages of the component - this should have a row for each usage~~ ✅
- ~~It should be simple to click the name of a prop or project to apply a filter to the data for that prop/project~~ ✅
- ~~Where we show source snippets or a file breakdown we should create a link to the code file, based on the repo URL. Eg if we know it's a github.com repo, build a URL matching GitHub's link structure to take you to that code file~~ ✅

~~The same should apply to Function explorer, this should mirror the functionality of Component Explorer but for functions/args instead of component/props.~~ ✅
